<UserControl x:Class="KuaiyunClient.Views.NodesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="节点" FontSize="28" FontWeight="Bold" />
            <TextBlock Text="订阅加载完成后在这里选择线路"
                       Foreground="{StaticResource MutedTextBrush}"
                       Margin="0,6,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,22,0,14">
            <Button Content="刷新节点" Width="110" HorizontalAlignment="Left"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="RefreshButton_Click" />
            <TextBlock x:Name="SummaryText" Text="0 个节点"
                       Foreground="{StaticResource MutedTextBrush}"
                       HorizontalAlignment="Right" VerticalAlignment="Center" />
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <Grid>
                <TextBlock x:Name="EmptyText"
                           Text="尚未加载节点。登录并获取订阅后再显示真实节点。"
                           Foreground="{StaticResource MutedTextBrush}"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />

                <ListBox x:Name="NodesList" Visibility="Collapsed"
                         BorderThickness="0" Background="Transparent"
                         SelectionChanged="NodesList_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="120" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Type}"
                                               Foreground="{StaticResource MutedTextBrush}"
                                               FontSize="11" Margin="0,4,0,0" />
                                </StackPanel>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DelayMilliseconds, StringFormat={}{0} ms}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"
                                           Foreground="{StaticResource MutedTextBrush}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
